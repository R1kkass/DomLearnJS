let table = document.querySelector('#age-table')

let label = document.querySelectorAll('label')

let row = document.querySelector('table').rows[0].cells[0]

let form  = document.querySelector('form[name="search"]')

let inp = document.querySelectorAll('input')[document.querySelectorAll('input').length-1]